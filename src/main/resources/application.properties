# ===============================
# PROFILE: docker (POSTGRESQL) - PARA DOCKER COMPOSE
# ===============================
spring.config.activate.on-profile=docker

# BANCO DE DADOS - DOCKER (POSTGRESQL)
spring.datasource.url=jdbc:postgresql://postgres-db:5432/monitordb
spring.datasource.username=RM554460
spring.datasource.password=fiap100786
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/HIBERNATE - DOCKER
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Desabilita H2 no profile docker
spring.h2.console.enabled=false